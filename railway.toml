[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --timeout 1800 --workers 1 --threads 2 server:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
